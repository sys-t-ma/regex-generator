import 'package:fluent_ui/fluent_ui.dart';

class HomeFluentPage extends StatelessWidget {
  const HomeFluentPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Fluent'),
      color: Colors.red,
    );
  }
}
